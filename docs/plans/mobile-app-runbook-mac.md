# Mobile App — Mac Runbook (Phase 2 continuation)

*Written 2026-07-09 on the Windows machine. Everything that could be done
without a Mac / device is DONE and committed. This file is the handoff for
Claude Code running on David's Mac. Read docs/plans/mobile-app-plan.md first
for the architecture (thin shell in remote-URL mode — the webview loads
https://streamflaire.com; nothing is bundled).*

## State as of handoff (done on Windows — do NOT redo)

- Capacitor 8 installed; `capacitor.config.ts` (appId `com.streamflaire.hub`,
  remote URL, `errorPath` offline screen, UA suffix `StreamflaireHubShell`).
- `android/` and `ios/` projects generated and committed. Android build.gradle
  auto-applies google-services when `android/app/google-services.json` exists.
- Icons + splashes generated by `@capacitor/assets` from `assets/` sources
  (built from the PWA maskable icon; regen: `npx capacitor-assets generate
  --iconBackgroundColor "#0C0F0C" ...` — NOTE: it also emits an unwanted
  root `icons/` folder + `public/manifest.webmanifest`; delete those, we use
  `app/manifest.ts`).
- `native-shell/error.html` — offline retry screen (served via errorPath).
- `components/NativeShell.tsx` — mounted in the platform layout: status bar
  color, Android back button, notification-tap deep links (`data.url`), and
  client-facing links (`/hub /quote /pay /book /contract /portal`) open in the
  system browser. Exposes `getCapacitor()` / `nativePlatform()` helpers.
- Native push END-TO-END plumbed but **never executed** — verify everything:
  - `PushSubscription.platform` column ("web" | "ios" | "android"); for native
    rows `endpoint` holds the FCM device token, p256dh/auth are null.
  - `POST /api/app/push` accepts `{ platform, token }`.
  - `lib/push.ts` sends native subs via FCM HTTP v1 (self-signed JWT → OAuth,
    no SDK). Env-gated on `FIREBASE_SERVICE_ACCOUNT` (raw or base64
    service-account JSON) — set it on the Railway web service when David has
    the Firebase project. 404 from FCM prunes the row.
  - `usePush()` in `components/PushNotifications.tsx` auto-detects the shell:
    same toggle UI drives the Capacitor PushNotifications plugin.

## Firebase pieces (David may have done some — check before doing)

1. Firebase console → project (any name). No Analytics needed.
2. Add ANDROID app, package `com.streamflaire.hub` → download
   `google-services.json` → put at `android/app/google-services.json` (safe to
   commit — it's client config, not a secret).
3. Add IOS app, bundle `com.streamflaire.hub` → `GoogleService-Info.plist` →
   add to `ios/App/App/` (via Xcode "Add Files" so it joins the target).
4. Project settings → Service accounts → Generate new private key → set the
   JSON as `FIREBASE_SERVICE_ACCOUNT` on Railway (web service). Base64 it if
   multiline env gives trouble: `base64 -i key.json | tr -d '\n'`.
5. iOS ONLY, needs Apple Developer ($99): create an APNs auth key (.p8) in the
   Apple dev portal → upload to Firebase → Cloud Messaging → Apple app config.

## Mac work list

### A. First boot (no accounts needed)

1. `npm install` (repo root), then `npx cap sync`.
2. iOS deps: the generated project is **SPM-based** (`ios/App/CapApp-SPM/`,
   no Podfile) — `npx cap sync ios` resolves packages; no CocoaPods needed.
3. `npx cap open ios` → set the signing team (David's free personal team works
   for device installs; 7-day provisioning) → run on David's iPhone.
4. `npx cap open android` → run in an emulator (David has no Android phone).
5. Verify shell behavior on both:
   - App opens straight into streamflaire.com (login → dashboard).
   - Status bar is dark, no white flash after splash.
   - Android hardware back walks history; minimizes at the root.
   - A `/hub/...` client link opens in the system browser, not the shell.
   - Airplane mode + relaunch → the branded offline screen, Retry works.
   - **Safe areas (deferred from Windows — needs eyes on a device):** check
     the mobile tab bar and Atlas bubble against the home indicator / notch.
     If clipped: `viewport-fit=cover` on the viewport meta +
     `env(safe-area-inset-*)` padding in AppShell. Also check the login page.
   - **Turnstile inside the webview** (login page). If it blocks the native
     UA, options: allow the `StreamflaireHubShell` UA suffix in Cloudflare, or
     relax the widget for the app host. Do NOT weaken web protection.

### B. Native push (needs Firebase files from above)

1. iOS: in Xcode add capabilities **Push Notifications** + **Background Modes
   → Remote notifications**. Add the `firebase-ios-sdk` package via SPM
   (FirebaseMessaging product) — the plugin's iOS token is APNs by default;
   the AppDelegate must exchange it for FCM (see Capacitor push-notifications
   guide, "Firebase" section) so the `registration` event yields an **FCM**
   token our server can send to.
2. Android: `google-services.json` in place is all it takes (template
   auto-applies the plugin). Android 13+ runtime permission is handled by the
   plugin's `requestPermissions()`.
3. Test matrix (server sends only when `FIREBASE_SERVICE_ACCOUNT` is set on
   Railway):
   - Toggle on in My Profile inside the app → row appears in PushSubscription
     with platform ios/android and a token in `endpoint`.
   - Trigger a team-chat message from another account → notification arrives
     (background AND foreground).
   - Tap it → app opens `/app/chat` (data.url deep link).
   - Toggle off → row deleted.
   - iOS reviewer path: notifications still optional — never block usage.

### C. Store prep (Phase 3 — see mobile-app-plan.md)

- Privacy policy URL (required by both stores): **https://streamflaire.com/privacy** (live).
- Android first: Play Console ($25) → closed testing. Keystore: let Play App
  Signing manage it. Then deep links (assetlinks.json needs the Play signing
  cert SHA-256) + apple-app-site-association for universal links.
- iOS: Apple Developer enrollment ($99/yr, takes days). TestFlight before review.

## Gotchas learned on the Windows side

- `npx cap add ios` ran fine on Windows; only dependency install remains for Mac.
- The repo's Next dev/build is unrelated to the shell — the app loads the
  LIVE site. To test web changes in the shell, deploy them (Railway auto-deploys
  main) or point `server.url` at a dev tunnel temporarily (never commit that).
- Never run `prisma db push` without the Railway DATABASE_PUBLIC_URL pattern
  documented in CLAUDE.md; schema for push is already applied in prod.
